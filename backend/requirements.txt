# ─── Career OS — Production runtime dependencies only ───
# Slim list — every package below is imported in the codebase.
# Build memory: <200 MB. Build time on Render: ~2 min.

# ── Web framework ──
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart>=0.0.9     # required by FastAPI for multipart/form-data (PDF upload)
pydantic>=2.6.4

# ── Configuration ──
python-dotenv>=1.0.1

# ── HTTP client (async) ──
httpx>=0.27.0

# ── Database (Mongo) ──
motor==3.3.1
pymongo==4.5.0              # motor's underlying driver — version-pinned to match

# ── PDF parsing (CV upload) ──
pypdf>=4.3.0

# ── Email ──
resend>=2.0.0

# ── Payments ──
stripe>=8.0.0

# ── Emergent LLM (Claude + Gemini + OpenAI via Universal Key) ──
# CRITICAL: This package is not on public PyPI. Build command MUST include
# --extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
emergentintegrations==0.1.0
