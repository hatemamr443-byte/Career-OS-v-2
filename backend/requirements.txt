# ─── Career OS — Production runtime dependencies only ───
# Slim list — every package below is imported in the codebase.
# Build memory: <200 MB. Build time on Render: ~2 min.

# ── Web framework ──
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart>=0.0.9     # required by FastAPI for multipart/form-data (PDF upload)
pydantic>=2.6.4

# ── Configuration ──
python-dotenv>=1.0.1

# ── HTTP client (async) ──
httpx>=0.27.0

# ── Database (Mongo) ──
motor==3.3.1
pymongo==4.5.0              # motor's underlying driver — version-pinned to match

# ── PDF parsing (CV upload) ──
pypdf>=4.3.0

# ── Email ──
resend>=2.0.0

# ── Payments ──
stripe>=8.0.0

# ── Emergent LLM (Claude + Gemini + OpenAI via Universal Key) ──
# CRITICAL: This package is not on public PyPI. Build command MUST include
# --extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
emergentintegrations==0.1.0

# ── Error Monitoring (optional — only if SENTRY_DSN is set) ──
sentry-sdk[fastapi]>=2.0.0

# ── Optional LLM provider fallbacks ──────────────────────────────
# These are used when EMERGENT_LLM_KEY is unavailable.
# All imports are inside try/except blocks in llm_service.py
# so missing packages degrade gracefully (Emergent remains primary).
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0

# ── PDF generation (used in tests for PDF upload simulation) ──
reportlab>=4.0.0

# ── Optional observability ────────────────────────────────────
langfuse>=2.0.0         # AI observability — graceful noop if LANGFUSE_PUBLIC_KEY not set
                        # Remove from requirements if CI is slow (tracer degrades silently)
